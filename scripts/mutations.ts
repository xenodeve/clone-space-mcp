export interface Mutation {
  id: string;
  why: string;
  file: string;
  find: string;
  replace: string;
  suite: "bun" | "browser";
  expect: string;
}

// Browser-only defects are deliberately not in this corpus yet: the page.url() epoch leak and
// the epoch:0 counter need suite: "browser" wiring that this pass does not exercise.
export const MUTATIONS: Mutation[] = [
  {
    id: "publish-validation-unwired",
    why: "2d29a66 — deleting the call left 47 Bun + 22 browser green.",
    file: "src/capture/record.ts",
    find: "if (!staged.ok) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "captureHar refuses to publish when a HAR attachment corrupts a staged sidecar",
  },
  {
    id: "epoch-shape-rule-gone",
    why: "Issue #47 — removing the document-epoch shape check let URL-shaped and short epochs pass.",
    file: "src/capture/checkpoints.ts",
    find: "^epoch:[0-9A-Za-z_-]{16,}$",
    replace: ".+",
    suite: "bun",
    expect: "rejects a documentEpoch that carries the captured page URL",
  },
  {
    id: "epoch-binding-unchecked",
    why: "89650be — deleting the documentEpoch binding refusal left 61 Bun tests green.",
    file: "src/capture/checkpoints.ts",
    find: "if (binding.documentEpoch !== finalCheckpoint.primaryTarget.documentEpoch) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when the binding names the final checkpoint but a different document epoch",
  },
  {
    id: "checkpoint-artifacts-shape-unchecked",
    why: "Issue #68 — removing the artifacts array guard accepted a checkpoint with scalar artifacts.",
    file: "src/capture/checkpoints.ts",
    find: "if (!Array.isArray(checkpoint.artifacts)) return false;",
    replace: "if (false) return false;",
    suite: "bun",
    expect: "rejects a checkpoint whose artifacts is not an array",
  },
  {
    id: "openedat-binding-unchecked",
    why: "1b71ef2 — deleting the openedAt binding refusal left 61 Bun tests green.",
    file: "src/capture/checkpoints.ts",
    find: "if (binding.openedAt !== finalCheckpoint.openedAt) return { ok: false };",
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "refuses when the binding names the final checkpoint but a different openedAt",
  },
  {
    id: "mid-checkpoint-navigation-allowed",
    why: "Issue #47 — deleting the loaderId change refusal allowed mid-checkpoint navigation.",
    file: "src/capture/record.ts",
    find: "if (loaderIdAtClose !== loaderIdAtOpen) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "captureHar refuses to publish when the primary document changes while the checkpoint was open",
  },
  {
    id: "har-containment-unchecked",
    why: "Issue #47 — removing the HAR containment refusal allowed an association outside staging.",
    file: "src/capture/checkpoints.ts",
    find: "return isStrictlyWithin(resolvedRoot, resolvedPath);",
    replace: "return true;",
    suite: "bun",
    expect: "refuses when the HAR association resolves outside the staging root",
  },
  {
    id: "capabilities-association-required",
    why: "Issue #63 — removing the required run-level capabilities association accepted an unindexed artifact.",
    file: "src/capture/checkpoints.ts",
    find: "if (!isRunAssociation(checkpointsDocument.capabilities)) return { ok: false };",
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects a document missing the run-level capabilities association",
  },
  {
    id: "capabilities-file-required",
    why: "Issue #63 — removing the capabilities staged-file check accepted a missing or non-file artifact.",
    file: "src/capture/checkpoints.ts",
    find: "if (!(await isStagedRegularFile(stagingRoot, stagedDocument.capabilities as RunAssociation))) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when capabilities.json resolves outside the staging root",
  },
  {
    id: "capabilities-document-validated",
    why: "Issue #63 — removing capabilities document validation accepted invalid JSON and invalid schema documents.",
    file: "src/capture/checkpoints.ts",
    find: "if (capabilitiesDoc === undefined || !validateCapabilities(capabilitiesDoc).ok) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when capabilities.json has an unsupported schema version",
  },
  {
    id: "capabilities-flag-values-validated",
    why: "Issue #63 — removing tri-state value validation accepted values outside the contract.",
    file: "src/capture/checkpoints.ts",
    find: "if (flag in flags && !isCapabilityValue(flags[flag])) return { ok: false };",
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects capabilities with a value outside the tri-state contract",
  },
  {
    id: "capabilities-flags-required",
    why: "Issue #63 — removing the required-flag check accepted incomplete capability evidence.",
    file: "src/capture/checkpoints.ts",
    find: "if (!(flag in flags)) return { ok: false };",
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects capabilities missing one of the four flags",
  },
  {
    id: "capability-service-worker-detection",
    why: "Issue #64 — suppressing the ServiceWorker registration observation made a true dependency look absent.",
    file: "src/capture/record.ts",
    find: "serviceWorkerDependent = true;",
    replace: "serviceWorkerDependent = false;",
    suite: "browser",
    expect: "publishes true capabilities for the capability fixture",
  },
  {
    id: "capability-websocket-detection",
    why: "Issue #64 — suppressing the live WebSocket observation made a refused socket look absent.",
    file: "src/capture/record.ts",
    find: "webSocketDependent = true;",
    replace: "webSocketDependent = false;",
    suite: "browser",
    expect: "publishes true capabilities for the capability fixture",
  },
  {
    id: "capability-closed-shadow-root-detection",
    why: "Issue #64 — replacing the pierced DOM result with false hid a closed shadow root.",
    file: "src/capture/record.ts",
    find: "const closedShadowRootPresent = hasClosedShadowRoot(domDocument.root);",
    replace: "const closedShadowRootPresent = false;",
    suite: "browser",
    expect: "publishes true capabilities for the capability fixture",
  },
  {
    id: "capability-sourcemap-detection",
    why: "Issue #64 — replacing sourcemap declaration detection with false hid declared maps.",
    file: "src/capture/record.ts",
    find: "sourcemapDeclared: sourcemapDeclared ? true : scriptBodyUnreadable ? \"undetermined\" : false,",
    replace: "sourcemapDeclared: false,",
    suite: "browser",
    expect: "publishes true capabilities for the capability fixture",
  },
  {
    id: "sourcemap-post-sweep-fetch",
    why: "Issue #75 — skipping discovered map fetches omitted the map declared by a sweep-loaded script.",
    file: "src/capture/record.ts",
    find: "[...discoveredMapUrls].map((url) => context.request.get(url).catch(() => undefined)),",
    replace: "[...new Set<string>()].map((url) => context.request.get(url).catch(() => undefined)),",
    suite: "browser",
    expect: "publishes true capabilities for the capability fixture",
  },
  {
    id: "sourcemap-declaration-before-url-resolution",
    why: "Issue #75 — tying declaration evidence to a resolved URL misclassified malformed declarations.",
    file: "src/capture/record.ts",
    find: "sourcemapDeclared = true;",
    replace: "sourcemapDeclared = discoveredMapUrls.size > 0;",
    suite: "bun",
    expect: "publishes a sourcemap declaration whose URL cannot be resolved",
  },
  {
    id: "capability-observation-boundary",
    why: "Issue #75 — leaving dependency observation live let shutdown events cross the published boundary.",
    file: "src/capture/record.ts",
    find: "observingDependencies = false;",
    replace: "observingDependencies = true;",
    suite: "bun",
    expect: "ignores dependency events delivered after capabilities are observed",
  },
  {
    id: "target-creation-observation-boundary",
    why: "#117 review — the discovery listeners shipped without the boundary the capability flags have, so a target Chromium reported during context.close() was published as if the page had opened it.",
    file: "src/capture/record.ts",
    find: `if (!observingDependencies) return;
          const info = (payload as { targetInfo?: CdpTargetPayload }).targetInfo;`,
    replace: "const info = (payload as { targetInfo?: CdpTargetPayload }).targetInfo;",
    suite: "bun",
    expect: "stops recording targets once the observation window closes",
  },
  {
    id: "target-destruction-observation-boundary",
    why: "#117 review — same boundary on the destroy path: closing the context destroys every target it owns, and an unguarded handler stamps closedAt for our own teardown.",
    file: "src/capture/record.ts",
    find: `if (!observingDependencies) return;
          const targetId = (payload as { targetId?: unknown }).targetId;`,
    replace: "const targetId = (payload as { targetId?: unknown }).targetId;",
    suite: "bun",
    expect: "does not close a target on a destroy that arrives after the observation window",
  },
  {
    id: "snapshot-opener-asserted-unverified",
    why: "#117 deliverable 2 — carrying a snapshot's openerId verbatim aborts the whole capture, because validateTargets refuses a dangling reference and Target.getTargets reports targets whose opener has already closed.",
    file: "src/capture/targets.ts",
    find: "targetEntryFromCreated(openerRecorded ? info : { ...info, openerId: undefined }, openedAt),",
    replace: "targetEntryFromCreated(info, openedAt),",
    suite: "bun",
    expect: "keeps a snapshot target whose opener the run never recorded",
  },
  {
    id: "snapshot-drain-missing",
    why: "#117 deliverable 2 — without the drain, a target the snapshot proves is gone keeps no closedAt, because the listener guard filters on delivery time and the destroy may never be observed.",
    file: "src/capture/targets.ts",
    find: "entry.closedAt === undefined && drainable.has(entry.targetId) && !live.has(entry.targetId),",
    replace: "false,",
    suite: "bun",
    expect: "closes a target the snapshot proves is gone",
  },
  {
    id: "snapshot-drain-ignores-inflight-creation",
    why: "#117 review — Target.getTargets is not atomic: Chromium enumerates, then the response travels back. Without restricting the drain to what was recorded before the request, a target created in that window is stamped closed while it is still alive.",
    file: "src/capture/targets.ts",
    find: "entry.closedAt === undefined && drainable.has(entry.targetId) && !live.has(entry.targetId),",
    replace: "entry.closedAt === undefined && !live.has(entry.targetId),",
    suite: "bun",
    expect: "does not close a target created while the snapshot was in flight",
  },
  {
    id: "archive-association-containment-unchecked",
    why: "#123 - checkpoints.json is data that names the other artifacts and arrives with the archive, so a reader that resolves an association without a containment check reopens the hole publication already closed, on every read.",
    file: "src/archive/read.ts",
    find: "if (!isStrictlyWithin(root, path)) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses an association that resolves outside the archive root",
  },
  {
    id: "capture-tool-leaks-the-browser",
    why: "#124 - without the finally, every capture_page call over a long-lived MCP session leaves a Chromium process behind. A unit test that only checks the return value passes either way.",
    file: "src/serve/tools/capture-page.ts",
    find: "await browser.close();",
    replace: "void browser;",
    suite: "bun",
    expect: "releases the browser after a successful capture",
  },
  {
    id: "capture-tool-returns-the-har-as-the-archive",
    why: "#124 - captureHar returns the HAR path, not the archive root. Returning it as the archive sends the caller straight into inspect_archive's 'not an archive' refusal.",
    file: "src/serve/tools/capture-page.ts",
    find: "return { archive: outDir, har, url: params.url };",
    replace: "return { archive: har, har, url: params.url };",
    suite: "bun",
    expect: "returns the archive directory, not the HAR path inside it",
  },
  {
    id: "mcp-swallows-a-tool-failure",
    why: "#124 - mapping a thrown tool error to a normal result would report a failed capture as a success to the agent. The layer-3 round trip is the only place this is visible.",
    file: "src/serve/mcp.ts",
    find: "            isError: true,",
    replace: "            isError: false,",
    suite: "bun",
    expect: "a failing tool comes back as an error result, not a dead connection",
  },
  {
    id: "capture-tool-writes-over-an-existing-path",
    why: "#127 - captureHar tolerates an existing empty directory, which is fine for a library call and destructive for a tool an agent drives: an empty user profile or mount point is an empty directory.",
    file: "src/serve/tools/capture-guards.ts",
    find: "if (exists(resolved)) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses an outDir that already exists",
  },
  {
    id: "capture-tool-reaches-the-private-network",
    why: "#127 - protocol-only validation let a caller reach loopback, cloud metadata and intranet hosts with the host's network position rather than its own.",
    file: "src/serve/tools/capture-guards.ts",
    find: "    if (kind !== undefined) {",
    replace: "    if (false) {",
    suite: "bun",
    expect: "refuses a URL that resolves to a private or loopback address",
  },
  {
    id: "replay-falls-through-to-the-live-network",
    why: "#133 - notFound: abort is what makes replay evidence rather than optimism. Fall through instead and an incomplete archive silently reaches the origin, so a replay of a broken archive looks perfect.",
    file: "src/replay/replay.ts",
    find: 'notFound: "abort", url: "**/*"',
    replace: 'notFound: "fallback", url: "**/*"',
    suite: "browser",
    expect: "refuses to reach a live origin for what the archive is missing",
  },
  {
    id: "inspector-grades-capture-fidelity-cases-as-animations",
    why: "#128 - the fixture manifest declares nine cases and only four are animations. Grading the carousel, the lazy asset and the cross-origin stylesheet as missing animations turns a complete archive into a page full of red.",
    file: "src/serve/inspector.ts",
    find: '.filter((entry) => entry.kind === "animation")',
    replace: ".filter(() => true)",
    suite: "bun",
    expect: "shows a gap as a row, not as a count nobody compared",
  },
  {
    id: "transcript-recorder-installed-too-late",
    why: "#114 - the recorder has to be installed after goto and BEFORE the sweep. Installed later it records none of the scrolling the sweep drives, and the transcript is an empty file that looks like a page which never moved.",
    file: "src/capture/record.ts",
    find: "      await page.evaluate(new Function(TRANSCRIPT_INIT_SCRIPT) as () => void);",
    replace: "      void TRANSCRIPT_INIT_SCRIPT;",
    suite: "browser",
    expect: "records the scrolling the adaptive sweep drove",
  },
  {
    id: "transcript-scroll-listener-outside-the-capture-phase",
    why: "#115/§6.11 - a scroll on a nested container does not bubble to window. Without the capture phase the listener records the page and reports nothing for an inner scroller, which is indistinguishable from a container that never moved. The fixture's nested-scroller declares that case.",
    file: "src/capture/transcript.ts",
    find: "  }, true);",
    replace: "  }, false);",
    suite: "browser",
    expect: "records a nested container that scrolls itself",
  },
  {
    id: "snapshot-merged-in-arrival-order",
    why: "#122 - Target.getTargets promises no ordering. Merging in arrival order loses an opener relationship whenever the browser lists a child before its parent, because appendDiscovered can only assert an opener this run already recorded.",
    file: "src/capture/targets.ts",
    find: "      if (waitingOnSnapshotOpener) {",
    replace: "      if (false) {",
    suite: "bun",
    expect: "keeps an opener relationship the snapshot happened to list out of order",
  },
  {
    id: "discovery-not-scoped-to-this-run",
    why: "#122 - Target.setDiscoverTargets is browser-wide. Without scoping to this run's browserContextId, a concurrent capture sharing the browser pours its targets into this inventory and the archive describes a page it never visited.",
    file: "src/capture/targets.ts",
    find: "  return info.browserContextId === runContextId;",
    replace: "  return true;",
    suite: "browser",
    expect: "does not report targets from a concurrent capture sharing the browser",
  },
  {
    id: "capture-tool-unbounded-concurrency",
    why: "#127 - an MCP server is long-lived and an agent can call a tool as often as it likes. Unbounded, every concurrent call launches its own Chromium and a handful is a host with no memory left - a failure that belongs to the machine, so nothing inspecting an archive would see it.",
    file: "src/serve/tools/capture-page.ts",
    find: "  const run = captureQueue.then(async () => {",
    replace: "  const run = Promise.resolve().then(async () => {",
    suite: "bun",
    expect: "never runs two browsers at once",
  },
  {
    id: "post-sweep-drain-unbounded",
    why: "#127 - the drain runs after the sweep and so outside §6.10's wall-clock budget. One script read that never answers holds a browser open, and through the MCP tool the caller's request with it. A drain outside the budget is a hole in the budget.",
    file: "src/capture/budget.ts",
    find: "        timer = setTimeout(() => resolve(false), deadlineMs);",
    replace: "        timer = setTimeout(() => resolve(false), 3_600_000);",
    suite: "bun",
    expect: "stops waiting on work that outlasts the deadline",
  },
  {
    id: "archive-reader-echoes-runtime-errors",
    why: "#127 - a raw parse or permission error carries the runtime's own account of the bytes and of what exists where, and it travels back over MCP to whoever is driving the tool. The caller supplied the path; it did not ask for the parser's phrasing.",
    file: "src/archive/read.ts",
    find: "    throw new Error(`inspect_archive: ${fileName} in ${root} is not readable as JSON`);",
    replace: "    throw error;",
    suite: "bun",
    expect: "fails with a stable message, not a raw parser error",
  },
  {
    id: "allowed-roots-not-enforced",
    why: "#127 - a restriction a deployment sets and the tool ignores is worse than none, because the operator believes it holds. The mechanism is only worth adding if it is enforced.",
    file: "src/serve/tools/inspect-archive.ts",
    find: "    if (!params.allowedRoots.some((root) => isWithin(resolve(root), wanted))) {",
    replace: "    if (false) {",
    suite: "bun",
    expect: "refuses an archive outside the allowed roots",
  },
  {
    id: "fingerprint-key-gates-on-ordinal-and-text",
    why: "Issue #20, fixed in 44e2671 — ordinal and text hash were equality components of the bucket key, so one node inserted above a target made an element with a unique stable attribute unmatchable. It was reported `missing` and `replayOnly` at once.",
    file: "src/identity/fingerprint.ts",
    find: 'return [el.frameKey, el.tag, attrs].join("|");',
    replace: 'return [el.frameKey, el.tag, attrs, el.siblingOrdinal, el.textHash ?? ""].join("|");',
    suite: "bun",
    expect: "one unrelated node inserted above does not lose an element with a unique attribute",
  },
  {
    id: "request-normalization-association-required",
    why: "Issue #86 — dropping the required requestNormalization run association from checkpoints validation let an archive without a policy pass the manifest guard.",
    file: "src/capture/checkpoints.ts",
    find: 'if (!isRunAssociation(checkpointsDocument.requestNormalization)) return { ok: false };',
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects a document missing the run-level request-normalization association",
  },
  {
    id: "request-normalization-staged-file-required",
    why: "Issue #86 — dropping the staged regular-file check let an archive publish a policy that escapes the root through a symlink while remaining valid JSON.",
    file: "src/capture/checkpoints.ts",
    find: '!(await isStagedRegularFile(stagingRoot, stagedDocument.requestNormalization as RunAssociation))',
    replace: "false",
    suite: "bun",
    expect: "refuses when request-normalization.json resolves outside the staging root",
  },
  {
    id: "request-normalization-schema-unchecked",
    why: "Issue #86 — dropping the policy schema validation let a malformed or non-empty-key policy be published.",
    file: "src/capture/checkpoints.ts",
    find: "if (requestNormalizationDoc === undefined || !validateRequestNormalization(requestNormalizationDoc).ok) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when request-normalization.json is invalid JSON",
  },
  {
    id: "request-normalization-producer-ambiguity-unchecked",
    why: "Issue #91 — dropping the producer-side ambiguity check let a capture publish an archive whose policy collapses two distinct requests into one normalized match.",
    file: "src/capture/record.ts",
    find: 'const ambiguous = findAmbiguousNormalizedRequests(harEntries, volatileKeys);',
    replace: "const ambiguous: string[] = [];",
    suite: "bun",
    expect: "captureHar refuses when the policy collapses two distinct archived requests",
  },
  {
    id: "request-normalization-staged-ambiguity-unchecked",
    why: "Issue #91 — dropping the staged re-check let a tampered policy collapse distinct archived requests bypass the producer guard.",
    file: "src/capture/checkpoints.ts",
    find: "if (findAmbiguousNormalizedRequests(harEntries, volatileKeys as string[]).length > 0) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when the policy collapses distinct archived requests (ambiguity)",
  },
  {
    id: "commit-association-required",
    why: "Issue #103 — dropping the required commit run association from checkpoints validation let an archive publish without a commit marker.",
    file: "src/capture/checkpoints.ts",
    find: 'if (!isRunAssociation(checkpointsDocument.commit)) return { ok: false };',
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects a document missing the run-level commit association",
  },
  {
    id: "commit-marker-not-written",
    why: "Issue #103 — dropping the commit write let a capture publish without a marker a reader could verify against.",
    file: "src/capture/record.ts",
    find: "const commit = await buildCommit(stagingRoot, finalCheckpoint.checkpointId);",
    replace: "const commit = { schemaVersion: 0, producer: { name: \"x\", version: \"0\" }, createdAt: 0, artifacts: [], checkpointId: \"\" };",
    suite: "bun",
    expect: "captureHar publishes a commit marker that verifies the archive bytes",
  },
  {
    id: "termination-association-required",
    why: "Issue #104 — dropping the required termination run association from checkpoints validation let an archive publish without recording why capture stopped.",
    file: "src/capture/checkpoints.ts",
    find: 'if (!isRunAssociation(checkpointsDocument.termination)) return { ok: false };',
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects a document missing the run-level termination association",
  },
  {
    id: "termination-not-recorded",
    why: "Issue #104 — dropping the termination write let a capture publish without a record of its outcome, making a truncated capture indistinguishable from a complete one.",
    file: "src/capture/record.ts",
    find: "const terminationDecision = evaluateBudget(sweepBudgets, terminationStats);",
    replace: "const terminationDecision = { stop: false } as const;",
    suite: "bun",
    expect: "captureHar records budget-exceeded when the wall-clock cap is crossed",
  },
  {
    id: "termination-staged-file-required",
    why: "Issue #104 follow-up — dropping the termination staged regular-file check let a capture publish an archive whose association names a missing or directory termination.json (it is written before validation, unlike commit, so it can and must be checked here).",
    file: "src/capture/checkpoints.ts",
    find: 'if (!(await isStagedRegularFile(stagingRoot, stagedDocument.termination as RunAssociation))) {',
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when termination.json is missing",
  },
  {
    id: "targets-association-required",
    why: "Issue #117 — removing the required run-level targets association accepted an archive that never indexed the inventory.",
    file: "src/capture/checkpoints.ts",
    find: "if (!isRunAssociation(checkpointsDocument.targets)) return { ok: false };",
    replace: "if (false) return { ok: false };",
    suite: "bun",
    expect: "rejects a document missing the run-level targets association",
  },
  {
    id: "targets-containment-unchecked",
    why: "Issue #117 — removing the staged-file check let a targets association outside the staging root be read as this run's evidence.",
    file: "src/capture/checkpoints.ts",
    find: "if (!(await isStagedRegularFile(stagingRoot, stagedDocument.targets as RunAssociation))) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses when the targets association resolves outside the staging root",
  },
  {
    id: "targets-document-validated",
    why: "Issue #117 — removing inventory validation accepted a malformed inventory and one whose target closed before it opened.",
    file: "src/capture/checkpoints.ts",
    find: "if (targetsDoc === undefined || !validateTargets(targetsDoc).ok) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "refuses a target that closed before it opened",
  },
  // #178 — the resolver's rules. Every one of these makes it answer where it should decline, or
  // answer with a position that is off by one, and both produce a citation that cannot be checked.
  {
    id: "sourcemap-answers-from-another-line",
    why: "Issue #178 — segments are one flat ascending list, so a search that ignores the generated line returns the previous line's last segment for a position nothing maps.",
    file: "src/extract/sourcemap.ts",
    find: "if (segment.generatedLine !== targetLine) continue;",
    replace: "if (false) continue;",
    suite: "bun",
    expect: "never answers from a segment on another generated line",
  },
  {
    id: "sourcemap-guesses-past-the-column",
    why: "Issue #178 — without the break, the last segment on the line wins for every column, including columns before the first segment.",
    file: "src/extract/sourcemap.ts",
    find: "if (segment.generatedColumn > targetColumn) break;",
    replace: "if (false) break;",
    suite: "bun",
    expect: "returns nothing before the first segment of a line, rather than the nearest",
  },
  {
    id: "sourcemap-line-off-by-one",
    why: "Issue #178 — the format is 0-based and a stack frame is 1-based; the conversion is the single most likely defect in the module.",
    file: "src/extract/sourcemap.ts",
    find: "line: best.sourceLine + 1,",
    replace: "line: best.sourceLine,",
    suite: "bun",
    expect: "takes 1-based input and returns 1-based output",
  },
  {
    id: "sourcemap-field-count-unchecked",
    why: "Issue #178 — a malformed map decoded in part hands the caller positions it cannot tell from correct ones.",
    file: "src/extract/sourcemap.ts",
    find: "if (fields.length !== 1 && fields.length !== 4 && fields.length !== 5) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "rejects a segment with a field count the format does not define",
  },
  {
    id: "sourcemap-name-guessed-from-a-nameless-segment",
    why: "Issue #178 — a four-field segment carries no name, and reporting the running name index attributes a symbol to code that never named one.",
    file: "src/extract/sourcemap.ts",
    find: "nameIndex: fields.length === 5 ? nameIndex : -1,",
    replace: "nameIndex: nameIndex,",
    suite: "bun",
    expect: "leaves the name empty rather than guessing when the segment carries none",
  },
  {
    id: "sourcemap-generated-column-not-reset",
    why: "Issue #178 — the generated column resets each line while every other field accumulates; carrying it across lines shifts every segment after the first line.",
    file: "src/extract/sourcemap.ts",
    find: "    let generatedColumn = 0;",
    replace: "",
    suite: "bun",
    expect: "every segment round-trips: its generated position resolves back to its own source line",
  },
  {
    id: "sourcemap-excerpt-not-clamped",
    why: "Issue #178 — an unclamped window emits line 0 and lines past the end, so a citation near the top of a file shows text that does not exist.",
    file: "src/extract/sourcemap.ts",
    find: "const first = Math.max(1, line - context);",
    replace: "const first = line - context;",
    suite: "bun",
    expect: "clamps at the start of the file instead of emitting negative lines",
  },
  {
    id: "sourcemap-version-unchecked",
    why: "Issue #178 — v2 maps encode `mappings` differently, and decoding one as v3 produces positions that are wrong rather than absent.",
    file: "src/extract/sourcemap.ts",
    find: "if (map.version !== 3) return undefined;",
    replace: "if (false) return undefined;",
    suite: "bun",
    expect: "returns nothing for a version the format does not define",
  },
  // #176 — one entry per refusal rule. The rules are the safety half of bounded interaction, and a
  // rule that cannot be shown to fail alone is a rule nobody has evidence for.
  {
    id: "interaction-submit-type-allowed",
    why: "Issue #176 — a submit control pressed on an arbitrary page is the failure this policy exists to prevent.",
    file: "src/capture/interaction.ts",
    find: 'c.type === "submit" || c.type === "image"',
    replace: "false",
    suite: "bun",
    expect: "refuses a submit control by its own rule",
  },
  {
    id: "interaction-typeless-button-allowed",
    why: "Issue #176 — a <button> with no type submits its form; dropping the default is how a policy passes review and still submits.",
    file: "src/capture/interaction.ts",
    find: 'c.inForm && c.tag === "button" && c.type === ""',
    replace: "false",
    suite: "bun",
    expect: "refuses a bare button inside a form, which submits by default",
  },
  {
    id: "interaction-navigation-allowed",
    why: "Issue #176 — a click that navigates leaves every remaining selector pointing at a document they were never discovered on.",
    file: "src/capture/interaction.ts",
    find: 'if (c.href === "") return undefined;',
    replace: "if (true) return undefined;",
    suite: "bun",
    expect: "refuses an anchor that leaves the document",
  },
  {
    id: "interaction-download-allowed",
    why: "Issue #176 — a download attribute starts a file transfer the run cannot cancel.",
    file: "src/capture/interaction.ts",
    find: 'c.download ? "carries a download attribute" : undefined',
    replace: "undefined",
    suite: "bun",
    expect: "refuses a download by its own rule, not as a navigation",
  },
  {
    id: "interaction-new-context-allowed",
    why: "Issue #176 — a target=_blank click opens a page the driver is not watching and never closes.",
    file: "src/capture/interaction.ts",
    find: 'c.target !== "" && c.target !== "_self"',
    replace: "false",
    suite: "bun",
    expect: "refuses an element that opens another browsing context",
  },
  {
    id: "interaction-file-picker-allowed",
    why: "Issue #176 — a native file dialog blocks the run with nothing in the transcript to say why.",
    file: "src/capture/interaction.ts",
    find: 'c.tag === "input" && c.type === "file"',
    replace: "false",
    suite: "bun",
    expect: "refuses a file picker, which opens a dialog the driver cannot dismiss",
  },
  {
    id: "interaction-cross-origin-allowed",
    why: "Issue #176 — acting on an off-origin control takes the run somewhere the capture policy never allowed.",
    file: "src/capture/interaction.ts",
    find: "c.sameOrigin ? undefined :",
    replace: "true ? undefined :",
    suite: "bun",
    expect: "refuses anything pointing off-origin, ahead of the navigation rule",
  },
  {
    id: "interaction-auth-wording-allowed",
    why: "Issue #176 — pressing a sign-in or wallet control on a stranger's page is the worst thing this tool could do.",
    file: "src/capture/interaction.ts",
    find: "AUTH_TERMS, AUTH_TERMS_TH",
    replace: "[], []",
    suite: "bun",
    expect: "refuses an authentication control by its label",
  },
  {
    id: "interaction-destructive-wording-allowed",
    why: "Issue #176 — the wording list is the only thing standing between the driver and a Delete button.",
    file: "src/capture/interaction.ts",
    find: "DESTRUCTIVE_TERMS, DESTRUCTIVE_TERMS_TH",
    replace: "[], []",
    suite: "bun",
    expect: "refuses a destructive control by its label",
  },
  {
    id: "interaction-wording-matches-substring",
    why: "Issue #176 — a substring match refuses 'For buyers' for containing 'buy'; a policy that cries wolf gets switched off.",
    file: "src/capture/interaction.ts",
    find: '"\\\\b" + term + "\\\\b"',
    replace: "term",
    suite: "bun",
    expect: "matches wording on a word boundary, not a substring",
  },
  {
    id: "interaction-refused-still-clicked",
    why: "Issue #176 — a refusal that is recorded but not enforced reads as a working policy in every report.",
    file: "src/capture/interaction.ts",
    find: 'if (kind === "click" && refusal !== undefined) continue;',
    replace: "if (false) continue;",
    suite: "bun",
    expect: "withholds the click from a refused candidate and records the refusal",
  },
  {
    id: "interaction-truncation-silent",
    why: "Issue #176 — a cap that truncates without counting reads afterwards as 'we covered everything'.",
    file: "src/capture/interaction.ts",
    find: "truncated[kind] += 1;",
    replace: "truncated[kind] += 0;",
    suite: "bun",
    expect: "bounds each kind and counts what the bound dropped",
  },
  {
    id: "interaction-plan-order-unstable",
    why: "Issue #176 — the gate drives the same plan on both sides; a plan that depends on array position makes the two sides disagree for no reason.",
    file: "src/capture/interaction.ts",
    find: "(a, b) => a.order - b.order || a.selector.localeCompare(b.selector),",
    replace: "() => 0,",
    suite: "bun",
    expect: "is deterministic — the same candidates in a different array order give the same plan",
  },
  {
    id: "interaction-navigation-postcondition-gone",
    why: "Issue #176 — the policy refuses what it can predict; a handler can still navigate, and driving on produces a transcript that is fiction.",
    file: "src/capture/interaction-drive.ts",
    find: "if (page.url() !== startedAt) {",
    replace: "if (false) {",
    suite: "bun",
    expect: "stops the moment the document navigates, rather than driving a page it never discovered",
  },
  {
    id: "interaction-pointer-cursor-hover-gone",
    why: "Issue #176 — without instrumentation the computed cursor is the only hover signal, and losing it drops hover coverage to zero silently.",
    file: "src/capture/interaction.ts",
    find: "|| candidate.pointerCursor",
    replace: "",
    suite: "bun",
    expect: "hovers on a pointer cursor alone, which is the only hover signal structure carries",
  },
  // #176, second pass — every one of these is a case an independent reviewer found and a probe
  // then confirmed against Chromium. They are the rules the first pass did not have.
  {
    id: "interaction-form-attribute-missed",
    why: "Issue #176 — `form=\"id\"` associates a control with a form it is not inside, so closest(\"form\") is null and a submit button reads as safe.",
    file: "src/capture/interaction.ts",
    find: "element.form instanceof HTMLFormElement || ",
    replace: "",
    suite: "browser",
    expect: "a button associated by form= submits although no form contains it",
  },
  {
    id: "interaction-title-label-missed",
    why: "Issue #176 — an icon button has no text, so a label read from text alone hands every wording rule an empty string.",
    file: "src/capture/interaction.ts",
    find: 'element.getAttribute("title") || ',
    replace: "",
    suite: "browser",
    expect: "an icon button's meaning is read from title, since it has no text to read",
  },
  {
    id: "interaction-aria-disabled-ignored",
    why: "Issue #176 — component libraries mark a control unavailable with aria-disabled while keeping it focusable; reading only `disabled` misses it.",
    file: "src/capture/interaction.ts",
    find: ' || element.getAttribute("aria-disabled") === "true"',
    replace: "",
    suite: "browser",
    expect: "an aria-disabled control is not a candidate at all",
  },
  {
    id: "interaction-transparent-visible",
    why: "Issue #176 — a fully transparent control still takes a click, and nothing else in the visibility check sees it.",
    file: "src/capture/interaction.ts",
    find: 'return style.opacity !== "0";',
    replace: "return true;",
    suite: "browser",
    expect: "a fully transparent control is not a candidate, however clickable it is",
  },
  {
    id: "interaction-discovery-not-isolated",
    why: "Issue #176 — a custom element with a throwing getter rejects the evaluate, so one bad element returns no candidates at all rather than one fewer.",
    file: "src/capture/interaction.ts",
    find: "} catch (error) { continue; }",
    replace: "} finally { void 0; }",
    suite: "browser",
    expect: "one element that throws while being read does not abort the whole discovery",
  },
  {
    id: "interaction-stale-element-driven",
    why: "Issue #176 — measured on www.firecrawl.dev, 28 of 32 selectors went stale while the plan ran; driving them anyway acts on whatever now occupies the position.",
    file: "src/capture/interaction-drive.ts",
    find: 'if (stale !== "") {',
    replace: "if (false) {",
    suite: "bun",
    expect: "never activates an element the policy would now refuse",
  },
  {
    id: "interaction-stale-label-unchecked",
    why: "Issue #176 — an insertion can leave a :nth-child path pointing at a different control, and activation is where that does damage.",
    file: "src/capture/interaction.ts",
    find: 'if (action.kind !== "click") return "";',
    replace: 'if (true) return "";',
    suite: "bun",
    expect: "stops a click on an element the policy would now refuse — the drift that does damage",
  },
  {
    id: "interaction-title-masks-text",
    why: "Issue #176 — preferring an attribute over text judges `<button title=\"Open panel\">Delete account</button>` on the benign one and clicks it.",
    file: "src/capture/interaction.ts",
    find: "element.innerText || \"\"\n    ].join(\" \")",
    replace: "\"\"\n    ].join(\" \")",
    suite: "browser",
    expect: "a benign title does not mask destructive text",
  },
  {
    id: "selector-returned-without-checking-it-resolves",
    why: "#168 - measured on labs.chaingpt.org: 154 of 203 gsap-timeline nodes reported `div`, and they were 154 different elements. A selector nobody checked names two elements as readily as one, and a node a caller cannot locate is a node it cannot ask a follow-up about.",
    file: "src/extract/behaviour.ts",
    find: "    return ancestorPath(element);",
    replace: "    return element.tagName ? element.tagName.toLowerCase() : \"\";",
    suite: "browser",
    expect: "a class-less element gets a selector that resolves to exactly one element",
  },
  {
    id: "ancestor-path-stops-before-it-is-unique",
    why: "#168 - the walk exists to climb until the path resolves to one element. Returning the first candidate gives back exactly the ambiguous selector the walk was added to replace.",
    file: "src/extract/behaviour.ts",
    find: "      if (resolvesToOne(candidate)) return candidate;",
    replace: "      return candidate;",
    suite: "browser",
    expect: "every target resolves to exactly one element in the replayed page",
  },
  {
    id: "scrolltrigger-config-dropped",
    why: "#167 - all eight ScrollTrigger nodes on a real page reported dur/delay/ease null and nothing else. Without start, end, scrub, pin and toggleActions the node names the mechanism and stops, which is the same class of answer as `the page loads gsap.min.js`.",
    file: "src/extract/behaviour.ts",
    find: "        start: typeof vars.start === \"string\" ? vars.start : null,",
    replace: "        start: null,",
    suite: "browser",
    expect: "a ScrollTrigger node carries the configuration that explains it",
  },
  {
    id: "stack-frame-guessed-instead-of-dropped",
    why: "#173 - a frame naming no coordinate (eval, <anonymous>, a node: internal) cannot be resolved, and inventing one is the fabricated citation the pipeline plan refuses by name. Dropping it is what lets slice 6 report generated-source honestly instead of guessing an original.",
    file: "src/capture/instrument.ts",
    find: "    if (match === null) continue;",
    replace: "    if (match === null) { frames.push({ url: \"unknown\", line: 0, column: 0 }); continue; }",
    suite: "bun",
    expect: "drops a frame it cannot read rather than guessing",
  },
  {
    id: "sequence-gap-not-accounted-for",
    why: "#173 - a gap in seq is a loss. Accepting one the drop counter does not explain makes the loss silent, which is the single property the counter exists to prevent.",
    file: "src/capture/instrument.ts",
    find: "  if (gap > record.droppedObservations) return { ok: false };",
    replace: "  if (false) return { ok: false };",
    suite: "bun",
    expect: "refuses a document whose sequence gap is not accounted for by its drop counter",
  },
  {
    id: "observation-recorded-without-its-stack",
    why: "#173 - the stack is in the schema from the first version on purpose. Without it every observation is unresolvable, and adding it later cannot repair archives already captured. It is what turned gl.shaderSource into three@0.151.2/build/three.module.js:18723:4.",
    file: "src/capture/instrument.ts",
    find: "      stack: (new Error()).stack || \"\"",
    replace: "      stack: \"\"",
    suite: "browser",
    expect: "observes a shader, a canvas context and a listener, each carrying a stack",
  },
  {
    id: "drain-does-not-hand-over-the-buffer",
    why: "#173 - the drain hands the buffer over rather than copying it. A drain that leaves the array in place returns the same observations on every call, so a caller draining per checkpoint counts each one repeatedly.",
    file: "src/capture/instrument.ts",
    find: "  s.observations = [];",
    replace: "  void 0;",
    suite: "browser",
    expect: "draining twice does not return the same observations again",
  },
  {
    id: "extract-replays-without-the-observation-layer",
    why: "#173 - the graph says what moves; the observation layer says what the page does. Replaying without hooks leaves an agent unable to reach a shader, a canvas realm or the interaction surface at all, which is the effect and 3D half of the goal missing from the only tool that could carry it.",
    file: "src/serve/tools/extract-behaviour.ts",
    find: "    const replay = await replayArchive({ archive: params.archive, browser, instrument: true });",
    replace: "    const replay = await replayArchive({ archive: params.archive, browser });",
    suite: "browser",
    expect: "extract_behaviour returns the observation summary an agent can act on",
  },
  {
    id: "shader-origin-invented-when-the-stack-is-unreadable",
    why: "#173 - the innermost readable frame is the shader's origin, and a stack naming no coordinate must leave it absent. Substituting a placeholder gives an agent a citation nobody can check, which is worse than saying the origin is unknown.",
    file: "src/serve/tools/extract-behaviour.ts",
    find: "            origin: parseStackFrames(observation.stack)[0],",
    replace: "            origin: parseStackFrames(observation.stack)[0] ?? { url: \"unknown\", line: 0, column: 0 },",
    suite: "browser",
    expect: "extract_behaviour returns the observation summary an agent can act on",
  },
  {
    id: "unobserved-read-as-agreement",
    why: "#171 - a field only one side produced was never compared. Reading it as `equal` is how a scroll-only clone comes to look complete: 242 click listeners are registered, none is ever fired, and the gate reports agreement about behaviour it never observed.",
    file: "src/equivalence/classify.ts",
    find: "    if (!onLive || !onReplay) {",
    replace: "    if (false) {",
    suite: "bun",
    expect: "a field neither side observed is unobserved, never equal",
  },
  {
    id: "incomplete-verdict-collapsed-into-pass",
    why: "#171 - a residual that is empty only because half the fields were never observed is not a pass. Collapsing INCOMPLETE into PASS hands a caller reading one value the opposite of what the run showed.",
    file: "src/equivalence/classify.ts",
    find: "  const verdict = residual.length > 0 ? \"FAIL\" : inconclusive ? \"INCOMPLETE\" : \"PASS\";",
    replace: "  const verdict = residual.length > 0 ? \"FAIL\" : \"PASS\";",
    suite: "bun",
    expect: "unobserved does not enter the residual, and does not let the verdict claim PASS either",
  },
  {
    id: "allowlist-accepts-a-wholesale-matcher",
    why: "#171 - measured on www.chaingpt.org: it cannot serve Cannon_Exterior.hdr, the environment map of its 3D scene, so an API-level comparison passes while the scene renders unlit. A matcher covering every field is that mistake written down, which is why category 1 may not be allowed wholesale.",
    file: "src/equivalence/classify.ts",
    find: "  if (entry.field === \"*\" || entry.field.trim().length === 0) {",
    replace: "  if (false) {",
    suite: "bun",
    expect: "an allowlist entry may not match every field",
  },
  {
    id: "allowlist-entry-needs-no-reason",
    why: "#171 - an allowlist whose reasons live in someone's head is how a gate quietly stops testing. The rationale is what makes a wrong exemption catchable later.",
    file: "src/equivalence/classify.ts",
    find: "  if (entry.rationale.trim().length === 0) {",
    replace: "  if (false) {",
    suite: "bun",
    expect: "an allowlist entry with no rationale is refused at load rather than silently accepted",
  },
  {
    id: "coverage-claims-an-interaction-it-never-drove",
    why: "#171 - a coverage vector that claims interaction it never drove turns the honest small claim into the rubber stamp the plan warns about: 242 click listeners registered, none fired, and the report saying otherwise. Re-anchored on #162: the original `interaction: [0, 1]` stopped existing when #180 made the numerator what the clone actually performed, and MUTATION NOT APPLIED had been the entry's silent result on main since. The over-claiming direction it used to express has no falsifier on a fixture where every planned action succeeds — performed and planned are the same number there — so what this now measures is the other half: the numerator is what the clone drove, not a constant.",
    file: "src/equivalence/run.ts",
    find: '        Number(replayed["interaction.performed"] ?? 0),',
    replace: "        0,",
    suite: "browser",
    expect: "reports a verdict and a coverage vector for the fixture",
  },
  {
    id: "stability-baseline-ignored",
    why: "#171 - measured on three real sites: comparing the live page with itself put dom.elements, motion.gsap and even ScrollTrigger registrations in the residual. Without the baseline the gate reports those as clone defects, which is a detector that cries wolf on every run and therefore gets switched off.",
    file: "src/equivalence/classify.ts",
    find: "      baseline !== undefined &&",
    replace: "      false &&",
    suite: "bun",
    expect: "a field that differs against itself is unstable, not different",
  },
  {
    id: "data-sourcemap-fetched-anyway",
    why: "#165 - an inline `data:` sourcemap needs no fetch, and asking `context.request.get` for one can never be answered. Measured on www.chaingpt.org: two entries in the published HAR with no response and no failure, while the network drain correctly reported the page had nothing outstanding. With #156 that is enough on its own to report the whole archive incomplete.",
    file: "src/capture/record.ts",
    find: "                    if (resolved.protocol === \"http:\" || resolved.protocol === \"https:\") {",
    replace: "                    if (true) {",
    suite: "browser",
    expect: "does not fetch a sourcemap that is published inline as a data URI",
  },
  {
    id: "inspect-complete-ignores-termination",
    why: "#159 - `complete` answered the integrity question while the tool documents itself as answering whether the capture is complete. Measured on three real sites: `complete: true` next to `outcome: \"incomplete\"`, for archives missing GSAP plugins and hero videos. An agent that reads the headline boolean and stops is told the opposite of what the archive knows.",
    file: "src/serve/tools/inspect-archive.ts",
    find: "    complete: archive.integrity.ok && termination.outcome === \"complete\",",
    replace: "    complete: archive.integrity.ok,",
    suite: "bun",
    expect: "inspectArchive does not call an archive complete when the capture terminated incomplete",
  },
  {
    id: "storage-read-after-a-cross-origin-redirect",
    why: "#157 - storage is read from the page after navigation. When the page landed on a different origin those entries belong to that origin, and the allowlist was written for the requested one. Reading anyway publishes another origin's data under a label that is not its own - which is exactly what the whole-capture refusal this replaced existed to prevent.",
    file: "src/capture/environment.ts",
    find: "  const storageIsThisOrigin = primaryOrigin !== \"null\" && finalOrigin === primaryOrigin;",
    replace: "  const storageIsThisOrigin = primaryOrigin !== \"null\";",
    suite: "browser",
    expect: "publishes a capture that redirected cross-origin, without that origin's storage",
  },
  {
    id: "final-origin-escapes-the-address-policy",
    why: "#157 - the pre-flight address check only sees the URL the caller asked for. The refusal it used to be paired with is gone, so without this hook a public URL that redirects to 169.254.169.254 publishes the internal page to disk instead of being thrown away.",
    file: "src/capture/environment.ts",
    find: "    if (origin === primaryOrigin) continue;",
    replace: "    continue;",
    suite: "bun",
    expect: "capturePage refuses when a redirect lands on a private address",
  },
  {
    id: "capture-tool-supplies-a-permissive-origin-policy",
    why: "#157 - the guard in captureHar is only as good as the policy the tool hands it, and a reviewer pointed out the entry above cannot see that: emptying the callback leaves every anchor in src/capture intact. This is the other half - the tool keeps passing a policy, and the policy permits everything.",
    file: "src/serve/tools/capture-page.ts",
    find: "        await assertReachableUrl(origin, params.allowPrivateNetwork === true, params.resolveHost);",
    replace: "        void origin;",
    suite: "bun",
    expect: "capturePage refuses when a redirect lands on a private address",
  },
  {
    id: "outstanding-responses-do-not-affect-the-outcome",
    why: "#156 - three captures of labs.chaingpt.org reported complete/quiet-window while holding 5, 1 and 3 HAR entries with no response, one of them the jQuery the page's whole interaction layer needs. Without this, an archive missing what the page asked for is indistinguishable from one that has everything.",
    file: "src/capture/budget.ts",
    find: "  const outcome = naturalEnd && stats.unansweredRequests === 0 ? \"complete\" : \"incomplete\";",
    replace: "  const outcome = naturalEnd ? \"complete\" : \"incomplete\";",
    suite: "browser",
    expect: "does not report complete when a response was never received",
  },
  {
    id: "unanswered-requests-counted-by-a-falsy-status",
    why: "#156 - Playwright writes an unanswered request as status -1, which is truthy. Counting on falsiness reports zero for exactly the entries this exists to find.",
    file: "src/capture/record.ts",
    find: "      return typeof status !== \"number\" || status < 100;",
    replace: "      return !status;",
    suite: "browser",
    expect: "does not report complete when a response was never received",
  },
  {
    id: "an-outstanding-response-read-as-a-failure",
    why: "#156 - Playwright writes both a failed request and one still in flight at teardown with status -1, and only `_failureText` separates them. Reading every responseless entry as a failure empties the count that drives the outcome, so the archive defect this exists to report becomes invisible again.",
    file: "src/capture/record.ts",
    find: "      (response) => response?._failureText !== undefined,",
    replace: "      () => true,",
    suite: "browser",
    expect: "does not report complete when a response was never received",
  },
  {
    id: "unservable-entries-left-to-the-har-router",
    why: "#155 - routeFromHAR matches an entry with no response and then neither fulfils nor aborts, so the request hangs forever. Without this abort, five such entries on labs.chaingpt.org left DOMContentLoaded and load both unfired and every replay of that site timed out.",
    file: "src/replay/replay.ts",
    find: "  if (unservable.size > 0) {",
    replace: "  if (false) {",
    suite: "browser",
    expect: "does not stall on a HAR entry the archive has no response for",
  },
  {
    id: "unservable-detected-by-a-truthy-status",
    why: "#155 - Playwright writes an incomplete request as status -1, so a check for a falsy status misses it entirely. The sentinel is 'not a real HTTP status', not 'zero'.",
    file: "src/replay/replay.ts",
    find: "    (typeof status === \"number\" && status >= 100 ? servable : unservable).add(url);",
    replace: "    (status !== 0 ? servable : unservable).add(url);",
    suite: "browser",
    expect: "does not stall on a HAR entry the archive has no response for",
  },
  {
    id: "one-bad-entry-condemns-the-whole-url",
    why: "#155 - a HAR can hold a good GET and a still-open POST for one URL. Refusing the URL on the bad entry alone aborts an asset the archive can serve, which removes working motion from a replay that previously had it - a worse failure than the stall being fixed.",
    file: "src/replay/replay.ts",
    find: "  for (const url of servable) unservable.delete(url);",
    replace: "  void servable;",
    suite: "browser",
    expect: "still serves a URL the archive has a good entry for, alongside a bad one",
  },
  {
    id: "fallback-continues-to-the-live-network",
    why: "#155 - every request that is not unservable now passes through this handler. `continue()` sends it to the network instead of deferring to the HAR router, which breaks the guarantee the whole archive rests on and looks identical offline. Only a test with the origin still listening can tell the two apart.",
    file: "src/replay/replay.ts",
    find: "      await route.fallback();",
    replace: "      await (route as unknown as { continue(): Promise<void> }).continue();",
    suite: "browser",
    expect: "still refuses a live origin when the unservable handler is installed",
  },
  {
    id: "network-drain-not-awaited",
    why: "#156 second deliverable - without waiting for in-flight responses at the observation boundary, the context teardown turns them into HAR entries with no response. Measured on firecrawl.dev before this: 4 entries with no response and no recorded failure.",
    file: "src/capture/record.ts",
    find: "      networkDrainSettled = await networkDrain.idle(NETWORK_DRAIN_DEADLINE_MS);",
    replace: "      networkDrainSettled = networkDrain.inFlight() === 0;",
    suite: "browser",
    expect: "waits for a response that arrives after the sweep has ended",
  },
  {
    id: "network-drain-counts-only-completions",
    why: "#156 - a drain that never learns a request started is idle from the first instant and returns immediately, which looks exactly like a page with nothing outstanding. The failure is silent in every offline test.",
    file: "src/capture/record.ts",
    find: "      page.on(\"request\", () => networkDrain.started());",
    replace: "      page.on(\"request\", () => {});",
    suite: "browser",
    expect: "waits for a response that arrives after the sweep has ended",
  },
  // #180 — the seams' own rules. Everything that reads a sourcemap out of an archive is reading
  // bytes from a site nobody controls, and the graph must say what it cannot represent.
  {
    id: "archive-sources-escapes-the-archive",
    why: "Issue #178 — a `_file` of ../../../etc/passwd would have the extract phase read an arbitrary file off the machine running it.",
    file: "src/extract/archive-sources.ts",
    find: 'if (inside === "" || inside.startsWith("..") || resolvePath(root, inside) !== path) return undefined;',
    replace: "if (false) return undefined;",
    suite: "bun",
    expect: "refuses a body path that points outside the archive",
  },
  {
    id: "archive-sources-takes-the-first-mapping-url",
    why: "Issue #178 — a bundler appends its declaration last; taking the first lets any string literal in the file point the resolver at a map of its choosing.",
    file: "src/extract/archive-sources.ts",
    find: "return matches.at(-1)?.[1];",
    replace: "return matches.at(0)?.[1];",
    suite: "bun",
    expect: "takes the last sourceMappingURL, which is the one a bundler appends",
  },
  {
    id: "archive-sources-accepts-an-unparsed-map",
    why: "Issue #178 — a 404 page served at the map URL is not a sourcemap, and indexing it reports a script as mapped that cannot resolve anything.",
    file: "src/extract/archive-sources.ts",
    find: "if (map !== undefined) maps.set(url, map);",
    replace: "maps.set(url, map as SourceMap);",
    suite: "bun",
    expect: "ignores a declared map whose body is not a sourcemap",
  },
  {
    id: "archive-sources-redirect-not-followed",
    why: "Issue #178 - a stack names the URL the page asked for, and a CDN often answers a redirect. Measured on www.chaingpt.org: unpkg answers 301 for the bare package URL, the map lives at the target, and indexing only the target left 2 of that site's 6 shaders uncitable.",
    file: "src/extract/archive-sources.ts",
    find: "        maps.set(from, map);",
    replace: "        void map;",
    suite: "bun",
    expect: "follows a redirect, so a stack naming the requested URL still resolves",
  },
  {
    id: "archive-sources-absolute-path-allowed",
    why: "Issue #178 - relative() cannot express a path on another Windows drive, so it returns that absolute path unchanged and the containment test passes. Probed with the repo on D:, a _file naming a path on C: was ALLOWED. src/capture/redact.ts:156 already refuses isAbsolute for the same field.",
    file: "src/extract/archive-sources.ts",
    find: "  if (isAbsolute(content._file)) return undefined;",
    replace: "",
    suite: "bun",
    expect: "refuses an absolute body path, even one that lands inside the archive",
  },
  {
    id: "behaviour-graph-hides-what-it-cannot-represent",
    why: "Issue #169 - the graph reports what getAnimations() knows plus GSAP's registries, and a CSS transition is in neither. Measured: www.firecrawl.dev gives 12 nodes against 318 transition-carrying elements. A node count with nothing beside it reads as completeness.",
    file: "src/extract/behaviour.ts",
    find: "      cssTransitionElements += 1;",
    replace: "      cssTransitionElements += 0;",
    suite: "browser",
    expect: "states the motion it has no node for, so a node count cannot read as completeness",
  },
  {
    id: "archive-published-from-a-private-address",
    why: "Issue #162 - every check before this one resolves a hostname at a moment that is not the moment the socket opened, so a page-initiated fetch of 169.254.169.254, a name that rebinds between the pre-flight lookup and the navigation, and a same-host rebind all reached a published archive. Without this refusal the archive keeps the response body of whatever the page asked for on the host's own network.",
    file: "src/capture/record.ts",
    find: "    if (options.allowPrivateNetwork !== true) {",
    replace: "    if (false) {",
    suite: "bun",
    expect: "refuses an archive whose connections went to a private address",
  },
  {
    id: "private-address-not-normalized-for-brackets",
    why: "Issue #162 - Playwright writes IPv6 into the HAR bracketed. Measured on one fixture capture: 11 of 12 entries carried \"[::1]\" and one carried \"::1\", in the same file. isIP(\"[::1]\") is 0, so classifying before stripping the brackets reports the loopback address the guard exists to catch as public.",
    file: "src/capture/private-address.ts",
    find: "  const normalized = address.toLowerCase().replace(/^\\[|\\]$/g, \"\");",
    replace: "  const normalized = address.toLowerCase();",
    suite: "bun",
    expect: "classifies the bracketed IPv6 form Playwright writes into the HAR",
  },
  {
    id: "capture-tool-opt-in-reaches-the-archive-unasked",
    why: "Issue #162 - capture_page applies the caller's allowPrivateNetwork to the pre-flight lookup and to every navigation origin. If the archive check reads a constant instead of the caller's choice, both name-based refusals still fire and the one that covers a subresource the page fetched itself never does.",
    file: "src/serve/tools/capture-page.ts",
    find: "      allowPrivateNetwork: params.allowPrivateNetwork === true,",
    replace: "      allowPrivateNetwork: true,",
    suite: "bun",
    expect: "refuses an archive whose connections went to a private address",
  },
  {
    id: "link-local-narrowed-to-the-fe80-prefix",
    why: "Issue #162 review - link-local is fe80::/10, first hextet fe80 through febf. Matching the literal prefix \"fe80:\" reports fe81:: to febf:: as somewhere on the internet, so an address inside the block the guard names is published.",
    file: "src/capture/private-address.ts",
    find: "    if (/^fe[89ab]/.test(normalized)) return \"link-local\";",
    replace: "    if (normalized.startsWith(\"fe80:\")) return \"link-local\";",
    suite: "bun",
    expect: "classifies the whole of fe80::/10, not only addresses beginning fe80",
  },
  {
    id: "ipv4-mapped-classified-only-in-dotted-form",
    why: "Issue #162 review - ::ffff:7f00:1 and ::ffff:127.0.0.1 are the same address. Matching only the dotted tail makes the verdict depend on which spelling the reporter printed, which is the same class of mistake as not stripping the brackets Playwright writes.",
    file: "src/capture/private-address.ts",
    find: "    const mappedHex = normalized.match(/^::ffff:([0-9a-f]{1,4}):([0-9a-f]{1,4})$/);",
    replace: "    const mappedHex = null;",
    suite: "bun",
    expect: "classifies an IPv4-mapped address written in hexadecimal",
  },
  {
    id: "settle-stops-at-the-first-agreeing-pair",
    why: "Issue #182 - the gate used to stop sampling at the first pair of equal readings and call that settled. Measured on six real series from labs.chaingpt.org, that reads 59 on a page that rests at 52, because an agreeing pair is a plateau. Restoring an early stop makes the digest read whichever plateau the sampling phase landed on, which is the non-reproducible verdict.",
    file: "src/equivalence/settle.ts",
    find: "  if (samples.length < repeats) return false;",
    replace: "  repeats = 2; if (samples.length < repeats) return false;",
    suite: "bun",
    expect: "no k reads the page's resting value, because the opening plateau is longer than the transition",
  },
  {
    id: "settled-reading-taken-from-the-first-sample",
    why: "Issue #182 - the digest must read the END of the budget. Reading anywhere else reintroduces the phase dependence the budget exists to remove, and the first sample is the entry plateau on every one of the six measured series.",
    file: "src/equivalence/settle.ts",
    find: "  const last = samples[samples.length - 1];",
    replace: "  const last = samples[0];",
    suite: "bun",
    expect: "reads the same value on every one of the six real series",
  },
  {
    id: "unsettled-motion-published-and-compared",
    why: "Issue #182 review - publishing motion.settled: false beside a mid-transition reading does not stop it being compared: the classifier compares every key it is given, so two runs that end mid-transition in the same way classify equal and hand out a PASS the measurement did not earn. The readings must be absent, because an absent key is already unobserved.",
    file: "src/equivalence/run.ts",
    find: "    ...(motionSettled",
    replace: "    ...(true",
    suite: "browser",
    expect: "a motion reading that never settled is not compared at all",
  },
  {
    id: "post-scroll-counts-read-once",
    why: "Issue #182/#187 - the gate took exactly one sample of the reading after the scroll pass. Measured on labs.chaingpt.org, three runs of live against replay: that one sample read dom.elements as 2821 live and 2819 on the clone on two of three runs, while every reading after it agreed at 2767 for fourteen straight samples. Publishing an unsettled count is publishing the difference the sampling made.",
    file: "src/equivalence/run.ts",
    find: "    ...(afterScrollSettled",
    replace: "    ...(true",
    suite: "browser",
    expect: "a motion reading that never settled is not compared at all",
  },
];
